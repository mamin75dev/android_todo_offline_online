<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shariat.mysuperapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MySuperApp">
        <activity android:name=".UI.WeatherDetailsActivity"></activity>
        <activity android:name=".UI.WeatherActivity" />
        <activity android:name=".UI.VolleyActivity" />
        <activity android:name=".UI.HttpTestActivity" />
        <activity android:name=".UI.AddToDoOnlineActivity" />
        <activity android:name=".UI.OnlineActivity" />
        <activity android:name=".UI.AddToDoOfflineActivity" />
        <activity android:name=".UI.OfflineActivity" />
        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>